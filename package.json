{
  "name": "smart-token-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm install --workspace=backend --legacy-peer-deps",
    "start": "npm start --workspace=backend"
  }
}
